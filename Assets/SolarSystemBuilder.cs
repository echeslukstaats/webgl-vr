using UnityEngine;

/// <summary>
/// The solar system is now a single background image with clickable hit
/// regions over each planet, built and owned by <see cref="SystemMapUI"/>.
///
/// This component no longer builds anything. It is kept only so the existing
/// GameObject in the scene does not report a missing script; it is safe to
/// delete both this file and that component together.
/// </summary>
public class SolarSystemBuilder : MonoBehaviour {
}
